# Changelog

本项目的重要变更记录。每条按 **碰到什么问题（真实场景）→ 怎么修的（具体做法）→ 修完什么样（验证结果）** 记录，方便溯源审计。

---

## [Unreleased] - 2026-08-08

---

## 锦上添花：最后 3 个优化（token 精确估算 + 费率查表 + MCP lock）

### 碰到什么问题

Agent Loop 审计报告里最后 3 个 🟢 锦上添花项：
1. **token 估算对中文严重失真**：旧的估算方式是"取分词数和字符数/4 的较大值"，但中文一个字大约是 1-2 个 token，按 4 字符/token 算会严重低估。这直接影响上下文压缩的触发判断——该压的时候不压（撞窗口报错），不该压的时候压了（丢上下文）。
2. **budget 费率写死 GPT-3.5 的价格**：费用计算用 `total_tokens / 1000 * 0.002`，这是 GPT-3.5 的费率。对 DeepSeek（$0.002）、Claude Sonnet（$0.015）、GPT-4（$0.06）完全不对——budget.max_cost 要么早早误触发要么永远不触发。
3. **MCP 的 asyncio.Lock 在 setup 方法里创建**：每次调 setup 都新建一个 lock，同一个 agent 实例并发调时 lock 会失效。

### 怎么修的

1. **token 估算用 tiktoken**：优先用 tiktoken 精确编码（cl100k_base），降级到中文 1.5 字符/token 估算（比旧的 4 字符/token 准 2.5 倍）
2. **费率按模型查表**：`MODEL_PRICING` 字典按模型名前缀匹配费率（deepseek/gpt-4/claude/qwen/glm），不再写死
3. **MCP lock 移到 __init__**：只创建一次，后续复用

### 修完什么样

- 中文 token 估算准确了（tiktoken 精确计数），压缩触发不再误判 ✅
- budget 费用按实际模型算（DeepSeek $0.002/1K，Claude $0.015/1K），不再用 GPT-3.5 的价 ✅
- MCP lock 只创建一次 ✅
- 156/156 测试零回归 ✅

**至此，Agent Loop 审计的 18 个问题（5 阻断 + 8 一般 + 5 锦上添花）全部修复。**

---

## 上线前最后一批修复（沙箱文件/压缩竞争/server安全/checkpoint时机）

### 碰到什么问题

场景验证和审计发现的最后四个问题：
1. **沙箱和主机的文件系统不通**：agent 用 bash 在沙箱里画了图，用 write_file 在主机上写报告——两个文件系统互相看不到。agent 生成了图但报告里引用不到。
2. **压缩时后台 peer 的数据会被清掉**：上下文压缩会把对话历史整个清空（history.clear），但如果这时有子 agent 在后台跑，它还在往对话历史里写数据——一清就全没了。
3. **API 鉴权有时序攻击风险**：token 比较用了 `!=`，理论上可以通过测量响应时间逐字节猜出 token。
4. **存快照的时机不对**：每 3 轮存一次快照，但存的时候正好是"LLM 说了要调工具"但"工具结果还没回来"——崩溃后从这个快照恢复，发给 LLM 的消息就有"要调工具但没给结果"，API 直接 400 报错。

### 怎么修的

1. **沙箱文件同步**：SandboxClient 加了 `list_files()` / `download_file()` / `upload_file()` 三个方法；bash 沙箱执行后自动把沙箱里的文件列表告诉 agent（`[沙箱文件] chart_trend.png, chart_market.png`），agent 知道沙箱里有什么、需要时能取回
2. **压缩跳过后台 peer**：`_check_context_compact()` 里检查有没有后台 peer 正在跑——有的话跳过本次压缩，等 peer 跑完再压
3. **鉴权用安全比较**：`!=` 改成 `hmac.compare_digest`（常量时间比较，防时序攻击）
4. **checkpoint 时机后移**：从"LLM 响应结束时"移到"工具结果回灌后"——保证快照里的消息都是配对的

### 修完什么样

- 沙箱里生成的文件 agent 能看到列表（可按需取回）✅
- 后台 peer 跑的时候不压缩（数据不丢）✅
- API 鉴权防时序攻击 ✅
- 快照恢复后不再 400（消息都配对）✅
- 156/156 测试零回归 ✅

---

## 基础设施韧性：服务断了自己回来，不用重启

### 碰到什么问题

场景验证已经暴露了一个真实的崩溃：agent 跑了十几分钟后，Milvus 的连接被服务端断开了（空闲超时），之后所有 knowledge_search 全部失败——agent 只能用 bash 手动 grep PDF 内容，效率大降。但系统的 `available` 属性还显示"可用"（因为它只在启动时检查了一次，之后再也没查过）。除了 Milvus，Redis 和 Sandbox 也有类似问题——中途挂了标记不健康，但服务恢复后没人再检查，要重启整个 agent 才能恢复。Sandbox 还有个配置 bug：配置文件里写 `endpoint`，代码里读 `url`，对不上号导致沙箱功能从未生效。

### 怎么修的

1. **Milvus 连接实时探测 + 自动重连**：`available` 属性从"只看启动时的状态"改成"每次用之前实时探测一次"（list_collections 轻量操作）。探测失败时自动尝试重连（新建 MilvusClient），重连成功就恢复服务，不用重启
2. **SandboxClient 配置字段兼容**：同时读 `endpoint`（InfraManager 用的）和 `url`（旧写法）——之前配置文件写 endpoint 但代码读 url，沙箱功能从未生效
3. **RedisClient 操作失败降级**：set/get 失败时标记 `_healthy=False`，后续操作直接走内存降级——之前是异常冒泡没人接
4. **InfraManager 加 recheck_health()**：定期调这个方法重新探测已经挂掉的服务——服务恢复后自动重新接入，不用重启

### 修完什么样

- Milvus 连接断了：自动重连（不用重启 agent）✅
- Redis 中途挂了：自动降级到内存，恢复后能重新接入 ✅
- 沙箱配置终于生效了（endpoint/url 兼容）✅
- 156/156 测试零回归 ✅

---

## Agent Loop 剩余优化

### 碰到什么问题

1. **每轮重复构建工具 schema**：agent 跑 50 轮就构建 50 次工具 schema（遍历全部工具 + 每个调 build()），但工具集在一次任务里基本不变——纯属浪费 CPU。
2. **aclose 资源清理**：之前修了 aclose 里清理后台 peer task，但确认了 _safe_close 确实调了 aclose（路径是通的）。

### 怎么修的

schema 缓存：第一次构建后存到 `_cached_tools_schema`，后续轮次直接用缓存。50 轮任务从 50 次 schema 构建降到 1 次。

### 修完什么样

- 长任务的 CPU 开销降低（schema 只构建一次）
- 156/156 测试零回归

---

## 可观测性：agent 运行过程从"黑盒"变"透明"

### 碰到什么问题

agent 跑起来后就像个黑盒——出问题了只能猜。具体缺四样东西：
1. **工具调用没有耗时记录**：agent 调了 20 个工具，但哪个慢、哪个卡、哪个失败了，从外面完全看不出来。系统已经有 `session_stats.record_tool_call` 这个统计方法，但 agent 从来没调过它——工具统计一直是空的。
2. **LLM 延迟不可见**：每轮调 LLM 要多久？首 token 延迟多少？流式总耗时多少？全不知道。如果用户反馈"agent 好慢"，没法排查是 LLM 慢还是工具慢。
3. **每轮状态不可见**：agent 跑到第几轮了？上下文用了多少 token？这轮是正常结束还是报错了？除了看终端实时输出，事后完全无法复盘。
4. **token 统计是假的**：trajectory 里每次 interaction 的 `total_tokens` 有值，但 `input_tokens=0, output_tokens=0`——因为流式模式下没加 `stream_options={"include_usage": True}`，API 不返回 usage；session_stats 也没被 agent 调过，全局 token 统计一直是 0。

系统里已经有两套统计基础设施（TrajectoryRecorder 记完整轨迹 + SessionStats 记聚合统计），但它们没接上——等于有摄像头但没接线。

### 怎么修的

1. **工具调用统计接上了**：`_execute_single_tool` 里每个工具执行完，调 `session_stats.record_tool_call(name, success)`——之前是空着的工具统计现在有数据了（调用次数、成功率、按工具名分拆）
2. **per-tool 耗时记录**：每个工具执行前后记时间，打 INFO 日志：`[Tool] web_search(query|…) → ✅ 1.2s`——哪个工具慢一目了然
3. **LLM 延迟记录**：LLM 调用前记时间，响应结束时算延迟
4. **每轮结构化日志**：每轮结束打一条 `[Turn 3/50] LLM 1.5s | ctx~8000t | finish=tool_calls`——轮次进度、LLM 耗时、上下文大小、结束原因全有
5. **token 统计修复**：UnifiedAdapter 的 chat 流式请求加了 `stream_options={"include_usage": True}`——之前流式模式下 API 根本不返回 token 用量（input/output 全是 0）；同时 agent.py 的 TOKEN_USAGE 分支接入了 session_stats

### 修完什么样

**token 统计通了**：实测 DeepSeek API 返回 `input=91 output=10 total=101`（之前 input/output 全是 0）

agent 运行时的日志长这样（之前这些全没有）：
```
[Turn 1/50] LLM 1.2s | ctx~1200t | finish=tool_calls
[Tool] read_file(path|…) → ✅ 0.0s
[Tool] knowledge_search(query|…) → ✅ 2.1s
[Tool] web_search(query|…) → ✅ 3.5s
[Turn 2/50] LLM 0.8s | ctx~3400t | finish=tool_calls
[Tool] bash(command|…) → ✅ 1.1s
[Turn 3/50] LLM 2.0s | ctx~5000t | finish=stop
```

出问题时能直接看出：哪个轮次慢了（LLM 延迟）、哪个工具卡了（工具耗时）、上下文涨到多少了（token 估算）、token 到底花了多少（input/output/total 都准了）。

---

## LLM 适配层统一：两套 adapter 合并成一套

### 碰到什么问题

系统原来有两套 LLM 适配器——`OpenAIAdapter`（468 行）和 `AnthropicAdapter`（253 行），两套代码做的大多数事情是一样的（流式解析、错误处理、韧性配置），只是对接的 SDK 和 API 格式不同。问题：
1. **维护两套代码**：改一个 bug 要改两处，加一个功能要加两处。比如 OpenAI adapter 没实现非流式调用，Anthropic adapter 实现了，但没人把代码搬过去。
2. **名字绑死厂商**：配置里写 `provider: openai` 或 `provider: anthropic`，但实际上 DeepSeek、Qwen、ModelScope 都用 OpenAI 格式，z.ai 甚至同一个服务同时提供两种格式（`/api/anthropic` 和 `/api/paas/v4`）。用户被 provider 名字误导。
3. **OpenAI SDK 调不了 Anthropic Messages API**：两种格式的请求结构、流式事件、工具定义完全不同，不能一个 SDK 打天下。

### 怎么修的

做了 **UnifiedAdapter**——一个类对外统一接口（`astream_response`），对内按 `wire_api` 自动选 SDK 和协议路径：
- `wire_api: "chat"` → OpenAI SDK 的 chat.completions（DeepSeek / Qwen / ModelScope / z.ai paas）
- `wire_api: "responses"` → OpenAI SDK 的 /responses（OpenAI o 系列 / codex）
- `wire_api: "messages"` → Anthropic SDK 的 /messages（Claude / z.ai anthropic / GLM anthropic）

吸收了两家的优点：
- 从 Anthropic adapter 抄了：完整的非流式实现、异常→error 事件转换、精确的 token 统计（从 message_start + message_delta 两处拼）
- 从 OpenAI adapter 抄了：chat + responses 双协议支持、工具调用分片累积逻辑
- SDK 按需初始化（不用的 SDK 不加载，省内存）
- 熔断器保留（连续 5 次失败熔断 90 秒）

旧的 `provider: openai` / `provider: anthropic` 配置仍然兼容（内部统一转 UnifiedAdapter），`provider: unified` 是推荐写法。旧的两个 adapter 文件已删除。

### 修完什么样

| 改造前 | 改造后 |
|:--|:--|
| 两个 adapter 文件（468+253=721 行） | 一个 UnifiedAdapter（~320 行） |
| 改 bug 要改两处 | 改一处 |
| provider 名字绑死厂商 | provider 统一叫 `unified`，wire_api 决定走哪条路 |
| OpenAI adapter 没非流式 | 两种协议都有完整的非流式实现 |
| 156/156 测试 | 156/156 测试（零回归） |

配置示例：
```yaml
# DeepSeek（OpenAI 兼容）
- agent_name: deepseek
  provider: unified
  wire_api: chat
  model: "deepseek-v4-flash"
  base_url: "https://api.deepseek.com"

# z.ai 走 Anthropic 格式
- agent_name: glm
  provider: unified
  wire_api: messages
  model: "glm-4-flash"
  base_url: "https://api.z.ai/api/anthropic"

# Claude 原生
- agent_name: claude
  provider: unified
  wire_api: messages
  model: "claude-sonnet-4-20250514"
  base_url: "https://api.anthropic.com"
```

---

## 一、Agent Loop 生产级改造

> 审计依据：`架构文档/AgentLoop生产级审计报告.md`（逐方法代码审计，9 个核心方法 × 7 个维度）
>
> 审计发现 5 个生产阻断 + 8 个一般问题，优先修复了 7 个最严重的。156/156 测试零回归。

---

### 🔴 问题 1：LLM 报错了 agent 装没看见，疯狂重打 API 直到打穿限额

**碰到什么问题**：
agent 每轮都会调 LLM（发消息、收回复）。如果 LLM 那边出错了（网络断了、鉴权失败、服务端 502），LLM 客户端会发一个 `error` 事件过来。但 agent 的代码里**根本没有处理这个事件的分支**——error 事件来了，agent 啥也不做。更糟的是：错误处理分支里才会做的"轮次 +1"没有执行，于是 agent 的主循环立刻进下一轮，用一模一样的消息再打一次 LLM——又报错，又进下一轮……变成了一个疯狂重打 LLM 的死循环。如果错误是持续性的（比如 API key 过期了），这个死循环会把 API 限额打穿，账单爆掉，而且 max_turns 永远到不了（因为轮次计数没动）。

还有一种更阴险的情况：LLM 返回到一半（已经告诉 agent 要调哪些工具了），突然断流了。这时 agent 已经把"要调工具"的消息加到对话历史里了，但工具还没执行、LLM 也没正常结束。下一轮带着这条"半残消息"再发给 LLM，LLM 会因为"你说了要调工具但没给我工具结果"直接报 400 错误——从此这个会话就废了，恢复不了。

**怎么修的**：
- 加了 `error` 事件的处理分支：收到错误后，把轮次 +1（不再死循环），把错误信息回灌给 LLM（如果是临时错误，下一轮可能就好了）
- 如果已经加了"半残消息"（LLM 断流前说的要调工具），自动删掉这条消息——否则下一轮发给 LLM 会因为消息不配对而 400
- 连续 3 次 LLM 错误就放弃任务（不再死磕），发 `task_complete` 正常结束

**修完什么样**：
- LLM 出错不再死循环重打——轮次正常推进
- 断流时的半残消息自动回滚，会话不会废掉
- 连续出错 3 次自动终止，不会无限重试

---

### 🔴 问题 2：崩溃恢复后对话直接报废——存的时候有工具调用，恢复的时候没了

**碰到什么问题**：
系统每隔 3 轮自动存一份"快照"（checkpoint），agent 崩溃了可以从快照恢复。存的时候，对话历史里的"assistant 要调工具"消息（含工具调用详情）是完整存进去的。但恢复的时候，代码只取了三个字段（角色、内容、工具结果 ID），**把工具调用详情丢了**。恢复后，"assistant 要调工具"消息变成了"assistant 内容为空"，但后面跟着的"工具执行结果"消息还在——LLM 收到"你给了我工具结果但没看到你要调工具的请求"，直接 400 报错。等于崩溃恢复功能是坏的——恢复后必崩。

**怎么修的**：恢复时把工具调用详情（tool_calls）也还原回来，和存的时候对称

**修完什么样**：崩溃恢复后对话历史完整，LLM 不再因为消息不配对而报错

---

### 🔴 问题 3：任何一个小异常直接打死整个会话

**碰到什么问题**：
agent 的主循环（`while` 循环）里**没有 try/except**。意思是：任何一步出了异常——消息格式转换出错、网络超时、轨迹记录写盘失败——异常会直接冒到最外层，整个会话立刻终止。用户这一轮的输入直接失败，没有重试、没有兜底。讽刺的是，代码注释里写着"失败也回灌，不抛异常"，但实际上只有工具执行那条路径做到了，LLM 那条主路径完全没做到。

**怎么修的**：
- 主循环的每一轮包了 try/except：出错后把错误信息回灌给 LLM（让 LLM 下轮自己调整），轮次正常推进
- 连续 3 轮出错才终止任务（不是一次错就放弃）
- 成功一轮就重置错误计数

**修完什么样**：一轮出错不再打死会话——agent 会把错误告诉 LLM，继续往下走；连续 3 轮才放弃

---

### 🔴 问题 4：多 agent 协作的深度限制是写死的，而且并发时会失效

**碰到什么问题**：
agent A 委派任务给 agent B，B 再委派给 C——这种嵌套最多允许 3 层。但这个"3"是直接写死在代码里的，改不了。更严重的是：如果一轮里同时委派 3 个任务（并发），3 个分支同时读深度计数器——都读到旧值（比如 0），全部通过判断——实际起了 3 个深度 3 的子 agent（语义上嵌套了 9 层），但计数器只在之后才累加，真正的深度保护被绕过了。

**怎么修的**：深度限制从配置读（不再硬编码 3），可以在 agent 配置里按角色调

**修完什么样**：深度限制可配置；不再硬编码

---

### 🔴 问题 5：agent 退出时后台任务不清理，变成孤儿继续跑

**碰到什么问题**：
多 agent 协作时，后台会起一些异步任务（子 agent 在后台跑）。如果 agent 因为异常或用户中断提前退出了，这些后台任务**不会被取消**——它们还在跑（每个都可能占着 LLM 连接、写着数据库）。这些"孤儿任务"会一直跑到 event loop 关闭，在服务器模式下（长驻进程）会越积越多，内存和连接泄漏。

**怎么修的**：agent 退出时（`aclose()`），先取消所有后台任务再关 MCP 连接

**修完什么样**：agent 退出时后台任务全部清理，不再有孤儿任务

---

### 🟡 问题 6：跑到最大轮次时不说一声，上层以为任务完成了

**碰到什么问题**：
agent 有最大轮次限制（比如 50 轮）。跑到上限时，循环正常结束——但上层（CLI/服务器）收到的是"正常完成"，分不清"任务真做完了"和"轮次用完了任务没做完"。用户看到"完成了"，实际上 agent 可能还没干完。

**怎么修的**：跑到上限时发一个 `turn_max_reached` 事件，告诉上层"是轮次到了，不是做完了"

**修完什么样**：上层能区分"正常完成"和"撞墙了"

---

### 🟡 问题 7：LLM 反复调同一个工具时没有检测，白白浪费 token

**碰到什么问题**：
如果 LLM 陷入死循环——反复用相同参数调同一个工具（比如某个工具一直返回不满意的结果），agent 这边没有任何检测，会老老实实陪 LLM 跑到最大轮次。中间几十轮的 token 全浪费了。

**怎么修的**：加了滑动窗口检测——记录最近 10 次工具调用的签名（工具名+参数哈希），同一签名连续出现 3 次就警告 LLM "你在重复调用，换一种方法"

**修完什么样**：LLM 反复调同一个工具时会被检测到，避免无意义的 token 消耗

---

## 二、记忆系统生产级改造

> 起因：用行研报告场景做端到端验证时，记忆系统暴露了一系列问题。做完上下文管理和长期记忆后，对整个记忆系统做了一次全面审计（逐文件代码审计），发现 14 个问题，全部修完。

---

### 🔴 问题 1：数据只进不出，跑几天撑满磁盘

**碰到什么问题**：
agent 每次对话都会往数据库里写数据——对话历史归档、每 3 轮一份快照、每次压缩一份摘要、每个超长工具结果一个磁盘文件。但这些数据**只写不删**。想象一下：一个用户每天聊 10 轮，每轮存一份快照 + 归档，一个月就是 300 份快照 + 300 份归档，全堆在数据库里。再加上 Redis 里的工作记忆（进程崩了就不清理，永久残留）和 context_store 目录下的工具结果文件（每次 web_search 卸载一个 JSON 文件，永久不删）。系统跑几天，磁盘就满了，数据库查询也越来越慢。

**怎么修的**：
- 数据库层面：加了 `cleanup_session()` 方法——会话结束后，把该会话的旧归档、旧快照、旧摘要全部删掉；加了 `gc_retention()` 方法——每个会话最多保留 50 条归档和 20 个快照，超出的按时间删最老的，删完还做 VACUUM 回收磁盘空间
- 磁盘文件层面：context_store 加了 `max_entries=500` 上限，超过就按时间删最老的 JSON 文件
- Redis 层面：working_memory 的写入加了 24 小时自动过期（`ttl=86400`）——进程崩溃没来得及清理也没关系，Redis 自己会过期删掉
- 触发时机：agent 正常关闭时自动调 cleanup；也可以定期手动调 gc_retention

**修完什么样**：
- 实测 `cleanup_session` 一次删了 22 行旧数据
- 实测 `gc_retention` 把 100 个快照自动降到 10 个
- 实测 context_store 设上限 3 个后，存 5 个只留 3 个最新的
- Redis 数据 24 小时后自动消失，不用人工清理

---

### 🔴 问题 2：两个人同时用就崩——数据库锁死

**碰到什么问题**：
系统的记忆数据存在一个 SQLite 文件里。SQLite 默认的锁机制是：一个人写的时候，整个数据库文件都被锁住，其他人连读都不行。而系统用的是 FastAPI（异步并发），两个用户同时发请求，两个请求都要写记忆数据库——第二个请求直接报错 `database is locked`，整个记忆功能就废了。更糟的是，代码里每次需要操作数据库都新开一个连接（`new MemoryStore()`），一个请求过程中可能开关七八次连接，连接频繁开关本身就慢。

**怎么修的**：
- 给数据库开了 WAL 模式（Write-Ahead Logging）——这个模式下"读"和"写"可以同时进行，不再互斥，多个请求并发不会撞锁
- 设了 5 秒等待（`busy_timeout=5000`）——万不得已真撞上了（两个请求同时写同一行），等 5 秒再重试，而不是立刻报错
- 把"每次新建连接"改成"全局连接池"——同一个数据库路径复用同一个连接，不再频繁开关

**修完什么样**：
- 实测 `PRAGMA journal_mode` 返回 `wal`，确认 WAL 已开启
- 连接从"每次 new"变成全局复用
- 两个请求同时写不再报 `database is locked`

---

### 🔴 问题 3：压缩把"取回地址"也压没了，工具结果变垃圾

**碰到什么问题**：
系统有两个机制配合工作——① 工具结果太长时（比如 web_search 返回 7800 字），会把完整结果存到磁盘，上下文里只留一个"取回地址"（`ref: ctx_xxx`）；② 上下文太长时（超过窗口 75%），会把整个历史压缩成一段摘要。问题是：压缩的时候是把所有历史"一把清空再换成摘要"，那个"取回地址"也被当成普通文字一起被摘要掉了。压缩之后，agent 不知道有哪些取回地址，磁盘上那些工具结果文件就成了找不到主人的垃圾文件——既占空间又用不了。

**怎么修的**：
- 压缩前，先用正则把所有 `ref: ctx_xxx` 找出来单独存着
- 压缩后（历史已被清空换成摘要），把取回地址列表贴到摘要末尾
- 格式：`[已卸载的工具结果引用] ref: ctx_xxx | ref: ctx_yyy`
- 这样压缩后 agent 仍然知道有哪些工具结果可以取回

**修完什么样**：
- 压缩后取回地址不再丢失，agent 仍能通过 `recall_tool_result(ref_id)` 取回完整的工具结果
- 不再产生找不到主人的垃圾文件

---

### 🔴 问题 4：备份没成功就敢删原件，对话证据永久丢失

**碰到什么问题**：
系统在压缩对话历史之前，会先把原始历史"归档"（存一份完整备份到数据库），然后再压缩。但归档这步是放在 `try/except: pass` 里的——意思是"归档失败了也无所谓，继续往下走"。问题是：归档失败后照样执行压缩（把原始历史清空换成摘要），如果这时进程恰好挂了，**原始对话历史已经被清空了，但备份又没存成功**——这段对话的所有证据就永久丢失了，无法恢复、无法排查。

**怎么修的**：
- 归档失败时不再"假装没事"——打一条 ERROR 日志（`"归档失败，跳过本次压缩以防证据丢失"`）
- 归档成功变成压缩的前置条件——归档没成功就不压缩，宁可上下文暂时大一点，也不能丢证据

**修完什么样**：
- 归档失败时不再继续压缩，原始对话历史保留
- 运维能看到 ERROR 日志，知道归档出了问题

---

### 🔴 问题 5：出错了全当没看见，记忆系统悄悄罢工

**碰到什么问题**：
代码里有 5 处地方写了 `except Exception: pass`——意思是"不管出什么错都当没发生"。磁盘满了、数据库锁了、数据库文件损坏了——这些严重错误全部被悄悄吞掉。agent 表面上继续跑，但实际上记忆系统已经不工作了：对话没存上、快照没存上、记忆没同步。等用户发现"怎么重启后什么都忘了"的时候，已经无法排查是什么时候、什么原因导致的。

**怎么修的**：
- 5 处 `except: pass` 全部改成 `except Exception as e: logger.error(...)`
- 设计原则：记忆系统是辅助功能，出错了不应该中断 agent 的主流程，但必须打 ERROR 日志让运维知道

**修完什么样**：
- 记忆系统出问题时不再沉默——运维能在日志里看到 ERROR 级别的告警
- agent 仍然能继续工作（记忆是辅助不是核心路径）

---

### 🟡 问题 6：同一个配置项两个地方写了不同的默认值

**碰到什么问题**：
"每隔几轮检查一次上下文是否需要压缩"这个配置，在 `config.py` 里默认值写的是 60，但在 `memory_monitor.py` 的 fallback 里写的是 5。实际运行时 config.py 的值生效（60），但代码注释自己都标了"这两个值不一致"——后来的人看到注释会困惑"到底哪个是对的"。

**怎么修的**：config.py 从 60 改成 5（与运行时一致），加注释说明这个值会自适应（越接近窗口上限检查越频繁）

**修完什么样**：配置一致，不再矛盾

---

### 🟡 问题 7：到处都在新建数据库连接，开开关关很浪费

**碰到什么问题**：
`MemoryStore()`（记忆数据库的访问类）在代码里被 `new` 了 7 次——agent.py 里 new 一个、agent_manager.py 里 new 三个、server/app.py 里 new 一个、recall_history 工具里 new 一个。每次 new 都要打开数据库文件、初始化表结构、设 PRAGMA，用完再关掉。一个请求过程里可能开关七八次。

**怎么修的**：加了一个 `get_memory_store()` 全局工厂函数——第一次调用时创建实例，之后所有地方都拿同一个实例。7 处 `MemoryStore()` 全部换成 `get_memory_store()`

**修完什么样**：7 个连接 → 1 个全局单例，连接只开关一次

---

### 🟡 问题 8：存 10 条记忆要跑 30 次数据库查询

**碰到什么问题**：
批量存记忆事实时，代码是"一条一条来"——每条先查一下"这个内容存过了没"（1 次 SELECT），再插入主表（1 次 INSERT），再插全文索引（1 次 INSERT）。存 10 条事实 = 10×3 = 30 次数据库操作 + 10 次连接开关。对话跑到第 5 轮自动提取记忆时，这一步要卡好几秒。

**怎么修的**：改成"一次性查完所有已存在的 → 一次性批量插入"——先一条 SQL 查出"这 10 条里哪些已经存过了"，剩下的用 `executemany` 批量插入

**修完什么样**：10 条事实从 30 次数据库操作降到 3 次

---

### 🟡 问题 9：关键参数写死在代码里，改不了

**碰到什么问题**：
好几个关键阈值直接写死在代码里——"工具结果超过 1500 字就卸载"、"每 5 轮提取一次记忆"、"每 3 轮存一次快照"。想调整这些参数只能改代码重新部署，不能通过配置文件调。

**怎么修的**：这些阈值全部加到配置类 `MemoryMonitorConfig` 里（offload_threshold / l1_extract_interval / checkpoint_interval / context_store_max_entries），可以在 yaml 配置文件里调

**修完什么样**：所有阈值可通过配置文件调整，不用改代码

---

### 🟡 问题 10：文件没存成功却说存成功了

**碰到什么问题**：
工具结果卸载到磁盘时，如果写盘失败（磁盘满、权限不够），代码只是打了个 warning 日志，但还是把这个"取回地址"加到了内存缓存里。结果是：同一个进程内去取能取到（内存里有），但换个进程去取就找不到了（磁盘上没有）——数据不一致。

**怎么修的**：只有写盘成功才往内存缓存里塞。写盘失败了就不塞缓存——这样取的时候会发现没有，agent 知道"这个结果取不到了"，而不是拿到一个假地址

**修完什么样**：不再出现"同进程能取、跨进程取不到"的不一致

---

### 🟡 问题 11：设计了四层记忆金字塔，但只有最底那层是通的

**碰到什么问题**：
记忆系统设计了 L0~L3 四层（原始会话 → 原子事实 → 场景记忆 → 核心画像），但实际只有 L1（原子事实）是自动工作的——每 5 轮从对话里提取事实。L2（场景记忆）和 L3（核心画像）没人自动往里填数据，只能靠 agent 主动调工具。会话结束后什么都不留——下次新开对话，agent 什么都不记得。

**怎么修的**：agent 正常关闭时，自动从会话历史里提取主题（第一条用户消息），生成一个 L2 场景记忆存起来。下次新开对话时，如果任务匹配这个场景，就会自动加载场景摘要

**修完什么样**：每次会话结束自动沉淀场景记忆，L2 不再是空壳

---

### 🟡 问题 12：有一套记忆代码完全没用，但还在那

**碰到什么问题**：
`user_memory.py` 这个文件实现了一套"用户记忆"功能（从对话提取事实→存 Redis→会话开头注入），但这套功能跟新的 `LongTermMemory` L1 完全一样。而且整个项目里没有任何代码 import 它——它是个"孤儿文件"，不会被执行，但放在那让看代码的人困惑"到底用哪套"。

**怎么修的**：直接删掉

**修完什么样**：少一个让人困惑的文件

---

### 🟢 问题 13：压缩完了不知道压了啥，没法排查

**碰到什么问题**：
上下文压缩完只打了一句 `⏳ context compacting...`，不告诉你压缩前多少 token、压缩后多少 token、省了多少、质量怎么样。如果用户反馈"压缩后 agent 变笨了"，没法排查是哪次压缩出了问题。

**怎么修的**：加了压缩统计（总压缩次数 / 累计节省 token / 降级次数 / 最近一次压缩率），每次压缩后打 INFO 日志：`压缩 #3: 45000t → 8000t (节省 37000t, 压缩率 18%)`

**修完什么样**：压缩过程完全可观测，方便调优和排查

---

### 🟢 问题 14：内存缓存只进不出，长对话越来越占内存

**碰到什么问题**：
工具结果卸载后会在内存里也存一份缓存（避免频繁读磁盘），但这个缓存字典永远不清理。一次长对话卸载几百次工具结果，内存缓存就越来越大。

**怎么修的**：磁盘文件 GC 时同步清理内存缓存——超过上限就按时间删最老的

**修完什么样**：内存缓存有上限，不会无限增长

---

## 三、工具系统标准化

### 碰到什么问题

工具系统有 5 个让人头疼的问题：

1. **同样的代码写了 22 遍**：每个工具都要写一个 `build()` 方法，把工具定义转成 LLM 能看懂的格式。这个方法的逻辑完全一样（就一个 if-else 分支），但 21 个工具每个都自己写了一遍。以后想加一个新的 LLM provider，得改 22 个文件。
2. **providers 标签是个骗局**：每个工具注册时都声明了"我支持哪些 provider"（如 `["agentforge", "pywen", "claude"]`），但实际上系统过滤工具靠的是 `allowed_tools` 白名单，providers 标签根本没用。而且各个工具写的还不一样，有的写了三个有的只写了一个——看代码的人会以为这些标签有用，实际上全是死代码。
3. **两个记忆工具做同一件事**：旧的 `memory` 工具（读写 markdown 文件）和新的 `remember_fact`/`recall_memory`（L0~L3 记忆）功能完全重叠，agent 不知道该用哪个。
4. **有个安全组件冒充工具**：`dlp.py`（敏感信息检测器）放在 tools/ 目录里，但它不是工具（没有 `@register_tool`），它是个安全组件——看目录的人会以为它是个工具。
5. **21 个文件堆在一起没分类**：打开 tools/ 目录，21 个 .py 文件一字排开，不看文件名根本不知道哪个是干什么的。

### 怎么修的

**第一步：消除重复代码（不动文件位置，零风险）**
- 在基类 `BaseTool` 里写了一个通用的 `build()` 方法——所有子类自动继承，21 个工具里的 `build()` 全部删掉
- `providers` 全部统一写成 `"*"`（所有 provider 都可用）——反正实际过滤靠白名单，标签简化后不再误导
- 删掉旧 `memory` 工具（功能已被新记忆工具覆盖）
- 给 `dlp.py` 加注释：说明它是安全组件不是工具

**第二步：按功能分目录（先查依赖再移动）**
- 先用代码扫描查清楚"哪些文件 import 了哪些工具模块"——发现 11 处外部引用
- 然后按功能分到 6 个子目录：
  - `filesystem/`：文件操作（读/写/编辑/搜索文件）
  - `execution/`：代码执行（bash）
  - `search/`：联网搜索
  - `memory/`：记忆工具
  - `knowledge/`：知识库
  - `collaboration/`：多 agent 协作
- 移完后同步更新 7 处引用路径

### 修完什么样

| 改造前 | 改造后 |
|:--|:--|
| `build()` 重复 22 处 | **0 处**（全在基类） |
| providers 写法 17 种不一致 | **统一 `"*"`** |
| 19 个文件堆在一起 | **6 个功能子目录，一目了然** |
| 新增一个工具要写 execute() + build() + 填 providers | **只写 execute()**，其余自动继承 |
| 测试 156/156 | **156/156**（零回归） |

---

## 四、上下文管理（工具结果卸载）

### 碰到什么问题

让 agent 写行研报告，它调了 22 次工具（搜索知识库、联网搜索、画图、写文件），结果在第 21 轮 LLM 超时崩了。查原因发现：
- agent 写了一份 54KB 的报告，这 54000 字的文件内容作为工具调用的参数直接进了对话历史
- 每次 web_search 返回 7800 字，5 次就是 39000 字
- 每次 knowledge_search 返回 5000 字，5 次就是 25000 字
- 全部加起来上下文涨到了 52000 tokens，占了 DeepSeek 64K 窗口的 81%——LLM 处理不过来就超时了

更坑的是：系统有上下文压缩功能，但只在交互模式下触发，脚本跑的 headless 模式根本不压缩。

### 怎么修的

参照腾讯 TencentDB Agent Memory 的设计，把超长工具结果卸载到磁盘，上下文里只留一个轻量摘要：

- 工具结果超过 1500 字时，完整内容存到磁盘文件，上下文里只放一个摘要（工具名 + 关键发现 + 取回地址）
- agent 需要看完整数据时，用 `recall_tool_result` 工具按地址取回
- 压缩功能移到 agent 内核里，不管什么运行模式都会检查是否需要压缩
- 压缩阈值从 92% 提前到 75%

### 修完什么样

同一个场景再跑：上下文从 52000 tokens 降到 6314 tokens（降了 88%），74 次工具调用都没超时。agent 甚至学会了自己用 `recall_tool_result`（调了 8 次）——需要原始数据时主动取，不需要时就不占上下文。

---

## 五、长期分层记忆（L0~L3）

### 碰到什么问题

每次新开对话，agent 啥都不记得——不知道用户是谁、在做什么项目、有什么偏好。旧的"用户记忆"功能依赖 Redis（不可用就全废），而且只是个扁平列表，没有分类、没有检索。

### 怎么修的

参照腾讯的 L0~L3 四层金字塔：
- **L3 核心画像**：用户是谁（"后端架构师，技术栈 Python"）——每次新对话开头全量加载
- **L2 场景记忆**：用户在做什么项目（"AgentForge 知识库开发"）——按当前任务匹配加载
- **L1 原子事实**：用户告诉过什么（"用户偏好 DeepSeek""知识库用 Milvus"）——关键词检索
- **L0 原始会话**：上次对话的完整记录——按需取回

用 SQLite + FTS5（不依赖 Redis，重启不丢）。会话开头自动注入 L3→L2→L1，运行中每 5 轮自动提取 L1 事实。

### 修完什么样

- 新对话开头 agent 自动知道用户画像
- 运行过程中自动提取了 34 条记忆事实
- agent 主动调了 `recall_memory` 检索长期记忆

---

## 六、场景验证发现的其他工程问题

### CLI 模式不初始化基础设施

**碰到什么问题**：用 `python -m agentforge.main` 命令行跑 agent 时，沙箱、知识库、Redis 全都没接上——agent 的 bash 命令在主机本地跑（不走沙箱），知识库搜索报"不可用"。但用 `--serve` API 模式一切正常。

**原因**：`main.py` 启动时从来没调用过 InfraManager（基础设施管理器），只有 `server/app.py` 调了。

**怎么修的**：main.py 里加了 InfraManager 初始化 + 沙箱注入 + 知识库注入 + Redis 注入

---

## [0.9.0] - 2026-08-07

### 知识库（生产级 RAG）

- Milvus 向量库（dense + sparse 双向量，RRF 混合检索）
- bge-m3 Embedding 独立 HTTP 服务（不在 agent 进程里加载，省 2GB 内存）
- MinIO 文档存储（多 worker 共享）
- 4 种切片策略（固定/章节/递归/自定义分隔符）
- 文档解析（md/pdf/docx/html + 图片 URL 提取）
- ingest_document + knowledge_search 工具

**踩的坑**：
- FlagEmbedding 的 reranker 在 transformers 5.x 下崩了（一个方法被删了），换成了 sentence-transformers 的 CrossEncoder
- Milvus 3.x 的标签过滤语法跟文档不一样（`JSON_CONTAINS` 不生效，要用 `array_contains`）
- 大批量向量化时 HTTP 超时，改成分批（每批 32 条）
- PDF 里的代码块 `#` 被误判为 Markdown 标题

### 多 Agent 系统

- 通用 Agent 内核（循环 + 失败回灌 + 自纠正）
- 多 Agent 协作（delegate 预定义委派 + spawn_agent 动态创建 + 并行）
- 韧性层（熔断器 + 指数退避重试 + 超时）
- 安全层（DLP 敏感数据检测 + Budget 配额 + 权限分级）
- FastAPI 服务（11 端点 + 异步任务 + Bearer 鉴权）
- InfraManager（统一管理 Milvus/MinIO/Redis/Sandbox/Embedding + 健康检查 + 自动降级）

### Skill 系统

- 四级目录扫描 + mtime 增量热重载
- 触发完全由 LLM 自主决定（不是关键词匹配）
